<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav-bar-items" aria-expanded="false">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Rails eCommerce", root_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="nav-bar-items">
      <ul class="nav navbar-nav">
        <li><%= link_to t(:products), "#" %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if is_logged_in? %>
          <li>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t :account %><b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t(:profile), "#" %></li>
              <li class="divider"></li>
              <li><%= link_to t(:logout), logout_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t(:log_in), login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
